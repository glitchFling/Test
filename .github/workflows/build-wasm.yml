name: Smart Build C++ → WASM

on:
  push:
    branches: ["**"]

jobs:
  # JOB 1: The Fast Validator (Node.js)
  validate:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.status }}
    steps:
      - uses: actions/checkout@v4
      - name: Run Node Validator
        id: check
        run: |
          TARGET=$(find . -name "ci_check.js" -print -quit)
          # We run the script and capture its decision
          # Note: The JS script below will set the 'status' output
          node "$TARGET"

  # JOB 2: The Heavy Compiler (Emscripten)
  build-wasm:
    needs: validate
    # ONLY runs if the first job outputted 'ready'
    if: needs.validate.outputs.should_run == 'ready'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest

      - name: Compile index.cpp
        run: |
          emcc index.cpp \
            -O3 \
            -fno-exceptions \
            -fno-rtti \
            -s DISABLE_EXCEPTION_CATCHING=1 \
            -s STANDALONE_WASM=1 \
            -nostdlib \
            -nostartfiles \
            -s ERROR_ON_UNDEFINED_SYMBOLS=0 \
            -Wl,--no-entry \
            -o index.wasm

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: index-wasm
          path: index.wasm
